{
  "name": "anthropic-sdk-vitest-tool-use",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sample: @anthropic-ai/sdk tool_use + Vitest",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
